# terraform.tfvars로 복사해서 실제 값을 채워 쓰세요 (terraform.tfvars는 .gitignore에 있어 커밋되지 않습니다)
#
# developer_ips: EKS 클러스터 API 공개 엔드포인트 접근을 허용할 IP CIDR 목록.
# 기본값이 없어서 이 값을 안 채우면 terraform apply/plan이 바로 에러 납니다.
#
# 본인 공인 IP 확인 후 /32로 채우세요:
#   curl -s ifconfig.me
developer_ips = ["YOUR_IP_ADDRESS/32"]
